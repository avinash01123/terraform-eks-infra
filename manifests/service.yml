---
apiVersion: v1
kind: Service
metadata:
  name: myapp
  namespace: metrics-server 
spec:
  selector:
    app: myapp
  ports:
    - port: 8080          
      targetPort: 80      
      protocol: TCP
  type: ClusterIP          
